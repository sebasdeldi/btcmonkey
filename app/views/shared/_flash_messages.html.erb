<% if flash.any? %>
  <div class="container">
    <% flash.each do |type, message| %>
      <div class="flash flash-<%= type %>" role="alert">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
