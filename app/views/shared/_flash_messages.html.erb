<% if flash.any? %>
  <div class="container">
    <% flash.each do |type, message| %>
      <% variant = type == 'notice' ? 'success' : (type == 'alert' ? 'error' : type) %>
      <%= render 'components/alert', variant: variant, message: message %>
    <% end %>
  </div>
<% end %>
